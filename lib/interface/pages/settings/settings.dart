import 'package:flutter/material.dart';

class SettingsViewer extends StatelessWidget {
  const SettingsViewer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("SettingsViewer"),
    );
  }
}