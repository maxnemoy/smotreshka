import 'package:flutter/material.dart';

class SearchViewer extends StatelessWidget {
  const SearchViewer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Search"),
    );
  }
}